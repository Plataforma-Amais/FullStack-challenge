const users = require('./users.models');
const schools = require('./schools.models');
const classes = require('./classes.models');
const teachers = require('./teachers.models');

module.exports = {
  users,
  schools,
  classes,
  teachers,
};
